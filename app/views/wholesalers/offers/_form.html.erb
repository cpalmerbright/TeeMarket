<%= form_for([@wholesaler, @offer]) do |f| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Batch" %>
    <%= @offer.batch.description %>
  </div>

  <div class="field">
    <%= f.label :wholesaler_id %>
    <%= @offer.wholesaler.name %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>


  <div class="field">
    <%= f.label :ignore %>
    <%= f.check_box :ignored %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
