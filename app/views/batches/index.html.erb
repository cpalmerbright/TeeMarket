<p id="notice"><%= notice %></p>

<h1>Batches</h1>

<table>
  <thead>
    <tr>
      <th>Manufacturer</th>
      <th>Quantity</th>
      <th>Fabric</th>
      <th>Color</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @batches.each do |batch| %>
      <tr>
        <td><%= batch.manufacturer %></td>
        <td><%= batch.quantity %></td>
        <td><%= batch.fabric %></td>
        <td><%= batch.color %></td>
        <td><%= link_to 'Show', manufacturer_batch_path(@manufacturer, @batch) %></td>
        <td><%= link_to 'Edit', edit_batch_path(batch) %></td>
        <td><%= link_to 'Destroy', batch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Batch', new_manufacturer_batch_path(@manufacturer) %>
